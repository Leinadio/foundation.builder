---
title: Composants UI
description: Bibliothèque de composants disponibles dans Foundation Builder
---

# Composants UI

Foundation Builder inclut une bibliothèque complète de composants UI modernes et accessibles, basés sur Radix UI et stylés avec Tailwind CSS.

## 🎨 Design System

### Couleurs

- **Primaire** : Bleu moderne pour les actions principales
- **Secondaire** : Gris pour les éléments secondaires
- **Succès** : Vert pour les confirmations
- **Erreur** : Rouge pour les erreurs
- **Avertissement** : Orange pour les alertes

### Typographie

- **Police principale** : Bricolage Grotesque
- **Police serif** : Bellefair
- **Tailles** : Du xs au 6xl avec échelle harmonieuse

## 🔐 Composants d'authentification

### AuthDialogForm

Modal de connexion/inscription avec basculement automatique.

```tsx
import { AuthDialogForm } from "@/components/auth";

<AuthDialogForm defaultMode="signin" onSuccess={() => console.log("Connexion réussie")} />;
```

### LoginForm

Formulaire de connexion standalone.

```tsx
import { LoginForm } from "@/components/auth";

<LoginForm onSubmit={(data) => handleLogin(data)} isLoading={false} />;
```

### RegisterForm

Formulaire d'inscription avec validation.

```tsx
import { RegisterForm } from "@/components/auth";

<RegisterForm onSubmit={(data) => handleRegister(data)} isLoading={false} />;
```

## 💳 Composants de paiement

### CheckoutButton

Bouton de paiement Stripe avec intégration complète.

```tsx
import { CheckoutButton } from "@/components/payment";

<CheckoutButton
  priceId="price_1234567890"
  onSuccess={() => console.log("Paiement réussi")}
  onError={(error) => console.error(error)}
>
  Acheter maintenant
</CheckoutButton>;
```

## 🎛️ Composants UI de base

### Button

Bouton polyvalent avec variantes et tailles.

```tsx
import { Button } from "@/components/ui/button";

<Button variant="default" size="lg">
  Cliquer ici
</Button>

<Button variant="outline" size="sm">
  Bouton secondaire
</Button>
```

### Card

Conteneur pour organiser le contenu.

```tsx
import { Card, CardHeader, CardContent, CardFooter } from "@/components/ui/card";

<Card>
  <CardHeader>
    <h3>Titre de la carte</h3>
  </CardHeader>
  <CardContent>
    <p>Contenu de la carte</p>
  </CardContent>
  <CardFooter>
    <Button>Action</Button>
  </CardFooter>
</Card>;
```

### Dialog

Modal pour les interactions importantes.

```tsx
import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/dialog";

<Dialog open={isOpen} onOpenChange={setIsOpen}>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>Confirmation</DialogTitle>
    </DialogHeader>
    <p>Êtes-vous sûr de vouloir continuer ?</p>
  </DialogContent>
</Dialog>;
```

### Input

Champ de saisie avec validation.

```tsx
import { Input } from "@/components/ui/input";

<Input type="email" placeholder="votre@email.com" value={email} onChange={(e) => setEmail(e.target.value)} />;
```

### Form

Composant de formulaire avec validation Zod.

```tsx
import { Form, FormField, FormItem, FormLabel, FormControl } from "@/components/ui/form";

<Form {...form}>
  <FormField
    control={form.control}
    name="email"
    render={({ field }) => (
      <FormItem>
        <FormLabel>Email</FormLabel>
        <FormControl>
          <Input {...field} />
        </FormControl>
      </FormItem>
    )}
  />
</Form>;
```

## 🎨 Composants avancés

### BentoGrid

Grille moderne pour afficher du contenu.

```tsx
import { BentoGrid, BentoGridItem } from "@/components/ui/bento-grid";

<BentoGrid>
  <BentoGridItem title="Fonctionnalité 1" description="Description de la fonctionnalité" icon={<Icon />} />
</BentoGrid>;
```

### Carousel

Carrousel pour les images et contenus.

```tsx
import { Carousel, CarouselContent, CarouselItem } from "@/components/ui/carousel";

<Carousel>
  <CarouselContent>
    <CarouselItem>
      <img src="/image1.jpg" alt="Image 1" />
    </CarouselItem>
  </CarouselContent>
</Carousel>;
```

### Timeline

Timeline pour les étapes et processus.

```tsx
import { Timeline, TimelineItem } from "@/components/ui/timeline";

<Timeline>
  <TimelineItem title="Étape 1" description="Description">
    <Icon />
  </TimelineItem>
</Timeline>;
```

## 🎭 Animations

### AnimatedThemeToggler

Bouton de basculement de thème avec animation.

```tsx
import { AnimatedThemeToggler } from "@/components/ui/animated-theme-toggler";

<AnimatedThemeToggler />;
```

### Framer Motion

Tous les composants supportent les animations Framer Motion.

```tsx
import { motion } from "framer-motion";

<motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.5 }}>
  Contenu animé
</motion.div>;
```

## 📱 Responsive Design

Tous les composants sont optimisés pour :

- **Mobile** : < 768px
- **Tablet** : 768px - 1024px
- **Desktop** : > 1024px

## ♿ Accessibilité

- Support complet des lecteurs d'écran
- Navigation au clavier
- Contraste respectant WCAG 2.1
- Focus visible et logique

## 🎨 Personnalisation

### Thèmes

Modifiez les couleurs dans `tailwind.config.ts` :

```typescript
theme: {
  extend: {
    colors: {
      primary: {
        DEFAULT: "#your-color",
        foreground: "#your-foreground-color",
      },
    },
  },
}
```

### Composants personnalisés

Créez vos propres composants en étendant les composants de base :

```tsx
import { Button } from "@/components/ui/button";
import { cn } from "@/lib/utils";

export function CustomButton({ className, ...props }) {
  return <Button className={cn("custom-styles", className)} {...props} />;
}
```
