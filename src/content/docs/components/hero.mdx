---
title: Hero
description: Le composant Hero est un composant de landing page qui permet de présenter une section d'accueil avec titre, description et call-to-action.
---

# Hero

Le composant Hero est une section d'accueil complète pour les landing pages. Il combine un titre, une description, des badges et un call-to-action dans une mise en page centrée et responsive.

---

## Aperçu du composant

import { Hero } from "@/blocks/landing-page/hero/ui";
import { Button } from "@/components/ui/button";

<Hero
  badge="Nouveau"
  title="Construisez votre application"
  titleHighlight="rapidement"
  description="Foundation Builder vous permet de créer des applications web modernes avec une architecture hexagonale, des composants réutilisables et un système de rendu dynamique."
>
  <Hero.CtaSection>
    <Button size="lg" className="h-12 min-w-72 text-lg">
      Commencer gratuitement
    </Button>
  </Hero.CtaSection>
</Hero>

---

## Structure du composant

### Container (index.tsx)

Le `HeroContainer` gère la logique métier et l'intégration avec les services :

```typescript
"use client";

import { Hero, HeroProps } from "@/blocks/landing-page/hero/ui";
import { Button } from "@/components/ui/button";
import { usePlausible } from "next-plausible";
import Link from "next/link";
import { Sailboat } from "lucide-react";

export type HeroContainerProps = HeroProps;

export function HeroContainer(props: HeroProps) {
  const plausible = usePlausible();
  return (
    <Hero {...props}>
      <Hero.CtaSection>
        <Button
          className="plausible-event-name=Best+Test h-12 min-w-72 text-lg"
          onClick={() => {
            plausible("Best Test");
          }}
          asChild
        >
          <Link href="/app" className="flex items-center justify-center gap-2">
            <Sailboat className="w-40 h-40" />
            Commencer gratuitement
          </Link>
        </Button>
      </Hero.CtaSection>
    </Hero>
  );
}
```

### Interface UI (ui.tsx)

Le composant `Hero` définit l'interface utilisateur pure :

```typescript
import { ReactNode } from "react";
import { Badge } from "@/components/ui/badge";
import { ProductHuntBadge } from "@/components/shared/product-hunt-badge";
import { Icons, Variant } from "@/components/shared/icons";
import { getChildrenByType } from "@/lib/get-children-by-type";

interface HeroAuthSectionProps {
  children: ReactNode;
}

function HeroCtaSection({ children }: HeroAuthSectionProps) {
  return children;
}

export interface HeroProps {
  badge: string;
  title: ReactNode;
  titleHighlight?: string;
  description: string;
  children?: ReactNode;
}

export function Hero({ badge, title, titleHighlight, description, children }: HeroProps) {
  const authSection = getChildrenByType<HeroAuthSectionProps>(children, HeroCtaSection);

  return (
    <section className="px-8 pt-24 md:pt-20 justify-center text-center flex flex-col mx-auto max-w-5xl items-center gap-10">
      <div className="flex flex-col items-center gap-4">
        <Badge variant="default">{badge}</Badge>
        <ProductHuntBadge />
      </div>
      <h1 className="text-4xl md:text-6xl font-semibold text-foreground">
        {title}
        {titleHighlight && <span className="text-primary ml-2">{titleHighlight}</span>}
      </h1>
      <p className="text-lg text-muted-foreground max-w-3xl">{description}</p>
      {authSection?.props.children}
      <Icons variant={Variant.ArrowDown4} color="var(--primary)" className="w-36 h-36 mt-6" />
    </section>
  );
}

Hero.CtaSection = HeroCtaSection;
```

---

## Propriétés

### HeroProps

| Propriété        | Type        | Requis | Description                                     |
| ---------------- | ----------- | ------ | ----------------------------------------------- |
| `badge`          | `string`    | ✅     | Texte du badge affiché en haut                  |
| `title`          | `ReactNode` | ✅     | Titre principal de la section                   |
| `titleHighlight` | `string`    | ❌     | Partie du titre à mettre en évidence            |
| `description`    | `string`    | ✅     | Description sous le titre                       |
| `children`       | `ReactNode` | ❌     | Contenu du call-to-action via `Hero.CtaSection` |

---

## Utilisation

### Dans le rendu dynamique

```typescript
// page-render-config.tsx
export const pageRenderConfig: SectionConfig[] = [
  {
    type: "section",
    components: [
      {
        id: "hero",
        component: "HeroContainer", // ✅ Utiliser le Container
        props: {
          badge: "Nouveau",
          title: "Construisez votre application",
          titleHighlight: "rapidement",
          description:
            "Foundation Builder vous permet de créer des applications web modernes avec une architecture hexagonale.",
        },
      },
    ],
  },
];
```

### Utilisation directe

```typescript
import { HeroContainer } from "@/blocks/landing-page/hero";

export function MyPage() {
  return <HeroContainer badge="Nouveau" title="Mon titre" titleHighlight="important" description="Ma description" />;
}
```

---

## Personnalisation

### Styles CSS

Le composant utilise des classes Tailwind CSS personnalisables :

- **Container** : `px-8 pt-24 md:pt-20 justify-center text-center flex flex-col mx-auto max-w-5xl items-center gap-10`
- **Titre** : `text-4xl md:text-6xl font-semibold text-foreground`
- **Titre en évidence** : `text-primary ml-2`
- **Description** : `text-lg text-muted-foreground max-w-3xl`

### Call-to-Action personnalisé

```typescript
<Hero badge="Nouveau" title="Mon titre" description="Ma description">
  <Hero.CtaSection>
    <div className="flex gap-4">
      <Button variant="default">Action principale</Button>
      <Button variant="outline">Action secondaire</Button>
    </div>
  </Hero.CtaSection>
</Hero>
```

---

## Fonctionnalités

### Analytics intégré

Le Container intègre Plausible Analytics pour le tracking des interactions :

```typescript
const plausible = usePlausible();

<Button
  className="plausible-event-name=Best+Test"
  onClick={() => {
    plausible("Best Test");
  }}
>
  Commencer gratuitement
</Button>;
```

### Navigation

Le bouton CTA redirige vers `/app` par défaut, mais peut être personnalisé via les props.

### Responsive Design

- **Mobile** : Titre en `text-4xl`, padding adapté
- **Desktop** : Titre en `text-6xl`, espacement optimisé
- **Largeur maximale** : `max-w-5xl` pour une lecture confortable

---

## Bonnes pratiques

### Utilisation des Container

- **Toujours utiliser `HeroContainer`** dans le rendu dynamique
- **Ne jamais utiliser `Hero` directement** dans la configuration
- **Le Container gère la logique métier** et l'intégration des services

### Contenu

- **Titre** : Court et percutant (2-8 mots)
- **Description** : Explicative mais concise (1-2 phrases)
- **Badge** : Information pertinente (Nouveau, Bêta, etc.)
- **CTA** : Action claire et motivante

### Accessibilité

- Le composant utilise des balises sémantiques (`<section>`, `<h1>`)
- Les couleurs respectent les contrastes d'accessibilité
- Le focus est géré par les composants Button intégrés
