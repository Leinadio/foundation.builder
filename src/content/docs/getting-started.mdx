---
title: Guide de démarrage
description: Configuration et premier projet avec Foundation Builder
---

# Guide de démarrage

Ce guide vous accompagne dans la configuration et l'utilisation de Foundation Builder pour votre premier projet.

## 📋 Prérequis

- Node.js 18+
- npm ou yarn
- Compte Firebase
- Compte Stripe (pour les paiements)

## 🚀 Installation

### 1. Cloner le projet

```bash
git clone <repository-url>
cd foundation.builder
```

### 2. Installer les dépendances

```bash
npm install
```

### 3. Configuration des variables d'environnement

Créez un fichier `.env.local` à la racine du projet :

```bash
cp .env.example .env.local
```

Remplissez les variables suivantes :

```bash
# Firebase Configuration
FIREBASE_API_KEY=your_api_key
FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_STORAGE_BUCKET=your_project.appspot.com
FIREBASE_MESSAGING_SENDER_ID=123456789
FIREBASE_APP_ID=1:123456789:web:abcdef

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Better Auth Configuration
BETTER_AUTH_SECRET=your_secret_key
BETTER_AUTH_URL=http://localhost:3000

# Email Configuration (Resend)
RESEND_API_KEY=re_...
```

### 4. Configuration Firebase

1. Créez un projet Firebase sur [console.firebase.google.com](https://console.firebase.google.com)
2. Activez l'authentification avec Email/Password et Google
3. Créez une base de données Firestore
4. Récupérez vos clés de configuration

### 5. Configuration Stripe

1. Créez un compte sur [stripe.com](https://stripe.com)
2. Récupérez vos clés API dans le dashboard
3. Configurez les webhooks pour les événements de paiement

## 🏃‍♂️ Premier lancement

```bash
npm run dev
```

Ouvrez [http://localhost:3000](http://localhost:3000) dans votre navigateur.

## 🎨 Personnalisation

### Changer les images

Remplacez les images dans le dossier `public/` :

- `public/images/` - Images de la landing page
- `public/videos/` - Vidéos de démonstration
- `public/icon/` - Icônes personnalisées

### Modifier le contenu

Éditez le fichier `page-render-config.tsx` pour personnaliser :

- Titre et description de la landing page
- Sections et composants affichés
- Liens de navigation

### Personnaliser les couleurs

Modifiez le fichier `tailwind.config.ts` pour changer :

- Couleurs principales
- Thèmes sombre/clair
- Typographie

## 🔧 Structure de base

### Pages principales

- `/` - Landing page
- `/sign-in` - Connexion
- `/sign-up` - Inscription
- `/docs` - Documentation

### API Endpoints

- `/api/auth/*` - Authentification
- `/api/users/*` - Gestion des utilisateurs
- `/api/payments/*` - Paiements Stripe

### Composants

- `src/components/auth/` - Composants d'authentification
- `src/components/ui/` - Composants UI de base
- `src/components/shared/` - Composants partagés

## 🧪 Tests

```bash
# Lancer les tests
npm run test

# Tests avec couverture
npm run test:coverage
```

## 📦 Build de production

```bash
# Build optimisé
npm run build

# Démarrer en production
npm run start
```

## 🚀 Déploiement

### Vercel (recommandé)

1. Connectez votre repository GitHub à Vercel
2. Configurez les variables d'environnement
3. Déployez automatiquement

### Autres plateformes

- **Netlify** : Compatible avec Next.js
- **Railway** : Déploiement simple
- **Docker** : Image Docker fournie

## 🆘 Résolution de problèmes

### Erreurs courantes

**Erreur Firebase** : Vérifiez vos clés de configuration
**Erreur Stripe** : Vérifiez vos clés API et webhooks
**Erreur de build** : Vérifiez les types TypeScript

### Support

- Consultez la [documentation](/docs)
- Ouvrez une issue sur GitHub
- Contactez l'équipe de développement
