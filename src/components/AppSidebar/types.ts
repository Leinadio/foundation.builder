/**
 * Interface pour les props du composant AppSidebar
 */
export interface AppSidebarComponentProps {
  /**
   * Nom du logo affiché dans la barre latérale
   */
  logoName: string;
  
  /**
   * Libellé de la section des workflows
   */
  workflowsSectionLabel: string;
} 